# Local Docker Neo4j password. Copy this file to .env before `docker compose up -d`.
NEO4J_PASSWORD=nemi-local-dev

# The RAG service reads these values when launched outside Docker.
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=nemi-local-dev
